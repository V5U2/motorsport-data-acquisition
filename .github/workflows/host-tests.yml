name: Fast Verification

on:
  push:
  pull_request:

jobs:
  fast-verification:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run fast repo verification
        run: ./scripts/verify-repo.sh --fast
